name: My Workflow
  on: push
  jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout code
          uses: actions/checkout@v2
        - name: Run tests
          uses: actions/test-runner@v1
          with:
            token: ${{ secrets.GITHUB_TOKEN }}